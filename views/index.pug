extends layout.pug

block content

  article.portfolio-intro

    h1 Hey, let's make the web great together!

    p This portfolio is meant to demonstrate my skills with web development - JavaScript, Express, NodeJS and technical abilities in general. Enjoy!

  article.portfolio-project-thumbs
    .projects-box.box.grid

      each project in projects
        .cell
          a(href=`/project/${projects.indexOf(project)}`)
            img.thumbnail(src=`${project.image_urls[0]}`, alt=`${project.project_name} Thumbnail`)
            h5 #{project.project_name}

